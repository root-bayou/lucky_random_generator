# ── Python ────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg
*.egg-info/
dist/
build/

# ── Virtual environments ───────────────────────────────────────
.env
.venv
venv/
env/
.python-version

# ── Project — personal / local only ───────────────────────────
# personal dev notes
claude.md
# local Crescendo manual entries (private)
src/data/
# abandoned Android build
mobile/
# SSH keys — never commit
id_rsa*
*.pem

# ── IDE ────────────────────────────────────────────────────────
.vscode/
.idea/
*.code-workspace

# ── Streamlit local config ─────────────────────────────────────
# .streamlit/ contains config.toml needed by Streamlit Cloud — DO NOT ignore
# .streamlit/secrets.toml  ← add this line if you ever use secrets

# ── OS ─────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
